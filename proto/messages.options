# nanopb options for messages.proto
# Caps the repeated Color array in SetPixelColors to 16 entries,
# keeping the maximum encoded message within the 256-byte firmware buffer.
SetPixelColors.colors   max_count:16
